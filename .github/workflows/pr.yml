name: Pull Request

"on":
  pull_request:
    paths:
      - 'src/**'
      - 'tests/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test PR workflow
        run: echo "hello, world"